vars /stats
    U stat_mode = 1

fn stat_init_bg()
    switch stat_mode
        case 0
            length_stats_init_bg()
            break
        case 1
            roll_probs_init_bg()
            break

fn stat_update_state_start()
    switch stat_mode
        case 0
            length_stats_update_state_start()
            break
        case 1
            roll_probs_update_state_start()
            break

fn stat_update_state_poll(U i, Bool poll)
    switch stat_mode
        case 0
            length_stats_update_state_poll(i, poll)
            break
        case 1
            roll_probs_update_state_poll(i, poll)
            break

fn stat_update_display_state()
    switch stat_mode
        case 0
            length_stats_update_display_state()
            break
        case 1
            roll_probs_update_display_state()
            break

fn stat_update_sprites(U oam_index) U
    switch stat_mode
        case 0
            return length_stats_update_sprites(oam_index)
        case 1
            return roll_probs_update_sprites(oam_index)

fn stat_update_bg()
    switch stat_mode
        case 0
            // length_stats_update_bg()
            break
        case 1
            roll_probs_update_bg()
            break

fn stat_update_bg_stop()
    switch stat_mode
        case 0
            // length_stats_update_bg_stop()
            break

fn stat_clean_bg()
    switch stat_mode
        case 0
            length_stats_clean_bg()
            break
        case 1
            roll_probs_clean_bg()
            break
